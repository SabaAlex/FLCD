{
    var a : Number, b : Number, counter : Number, gcd : Number;

    a = INPUT;
    b = INPUT;

    for (i = 1; i <= a AND i <= b; i = i SUM 1)

        if (a % i is 0 AND b % i is 0)

            gcd = i;

    OUTPUT gcd;
}